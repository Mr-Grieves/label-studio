# Copy this file to .env and fill in your real value -- .env is gitignored so
# it never ends up committed. docker-compose reads .env automatically.

# From Label Studio -> Account & Settings -> Access Token, once the
# label-studio container is up and you've created an account at localhost:8080.
LABEL_STUDIO_API_KEY=
